package xen

// NameToID attempts to convert a xen domain's name to its domid
func NameToID(name string) (uint, error) {
	return 0, nil
}

// IDToName attempts to convert a xen domain's domid to its name
func IDToName(id uint) (string, error) {
	return "", nil
}
